using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LineDrawing : MonoBehaviour
{
    // Variabelen voor het tekenen van de lijn
    // (bijv. een referentie naar de lijnrenderer, de start- en eindpunten)

    void Start()
    {
        // Initialisatie van lijntekeningen zoals het creëren van een LineRenderer
    }

    void Update()
    {
        // Functie om het tekenen van de lijn bij te houden op basis van de muisinvoer
        // Hier zal later de logica komen om een lijn te tekenen met de muis
    }
}